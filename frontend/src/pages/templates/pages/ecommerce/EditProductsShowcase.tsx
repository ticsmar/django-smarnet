import AddProductsShowcase from './AddProductsShowcase';

// Edit Products reuses same form structure; difference is the title and pre-filled data context.
export default function EditProductsShowcase() {
  return <AddProductsShowcase />;
}
