###### 1 -> Provide a better directory organization to the project. (It is large and confusing...)

###### 2 -> Provide a Docker image to Python, React.js and the Oracle Driver.

###### 3 -> Provide a CI tool, to run tests and mypy && ruff to check default code quality. Then push the codebase to new upper branch (dev -> test, but not to prod)

###### 4 -> Provide a CD tool, to automaticaly update the codebase (auto git pull in 'test' branch)
